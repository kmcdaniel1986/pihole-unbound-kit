#!/usr/bin/env bash
# Backup & restore Pi-hole + Unbound configuration
# Usage: sudo ./pihole-unbound-migrate.sh backup | restore <archive.tar.gz>
set -euo pipefail
# … (same content you already have) …
